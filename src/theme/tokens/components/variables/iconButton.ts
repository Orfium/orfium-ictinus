const iconButton = {
  padding: {
    value: '{spacing.4}',
    type: 'spacing',
    description: 'sets top, bottom, left right padding for icon buttons',
  },
} as const;

export default iconButton;

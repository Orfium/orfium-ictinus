import type React from 'react';

import type { LabelConfig } from '../Controls/Controls.types';

/** This is just a dummy component to be used in Storybook for showing the LabelConfig type on props */
export const LabelConfigStory: React.FC<LabelConfig> = () => null;

import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean, array, select, text } from '@storybook/addon-knobs';
import Icon from './Icon';
import Stack from '../storyUtils/Stack';
import PresentComponent from '../storyUtils/PresentComponent';

<Meta title="Design System|Icon" component={Icon} />

# Icon

A universal Icon component to select and style the icons encapsulated in the design system.

# Usage

```js
import { Icon } from 'orfium-design-system';

<Icon name={'add'} color={'primary'} size={14} />;
```

# Props

<Props of={Icon} />

# Icons Gallery

All the icons available for use.

<Preview>
  <Story name="Icons Gallery">
    <Stack>
        <PresentComponent name={'add'}>
            <Icon name={'add'} />
        </PresentComponent>
        <PresentComponent name={'fatArrowDown'}>
            <Icon name={'fatArrowDown'} />
        </PresentComponent>
        <PresentComponent name={'arrowDown'}>
            <Icon name={'arrowDown'} />
        </PresentComponent>
        <PresentComponent name={'check'}>
            <Icon name={'check'} />
        </PresentComponent>
        <PresentComponent name={'minus'}>
            <Icon name={'minus'} />
        </PresentComponent>
        <PresentComponent name={'arrowLeft'}>
            <Icon name={'arrowLeft'} />
        </PresentComponent>
        <PresentComponent name={'arrowRight'}>
            <Icon name={'arrowRight'} />
        </PresentComponent>
        <PresentComponent name={'arrowToRight'}>
            <Icon name={'arrowToRight'} />
        </PresentComponent>
        <PresentComponent name={'arrowToLeft'}>
            <Icon name={'arrowToLeft'} />
        </PresentComponent>
        <PresentComponent name={'ancient'}>
            <Icon name={'ancient'} />
        </PresentComponent>
        <PresentComponent name={'expenses'}>
            <Icon name={'expenses'} />
        </PresentComponent>
        <PresentComponent name={'financialChart'}>
            <Icon name={'financialChart'} />
        </PresentComponent>
        <PresentComponent name={'globalItem'}>
            <Icon name={'globalItem'} />
        </PresentComponent>
        <PresentComponent name={'income'}>
            <Icon name={'income'} />
        </PresentComponent>
        <PresentComponent name={'linked'}>
            <Icon name={'linked'} />
        </PresentComponent>
        <PresentComponent name={'musicNote'}>
            <Icon name={'musicNote'} />
        </PresentComponent>
        <PresentComponent name={'percentage'}>
            <Icon name={'percentage'} />
        </PresentComponent>
        <PresentComponent name={'restore'}>
            <Icon name={'restore'} />
        </PresentComponent>
        <PresentComponent name={'searchMusic'}>
            <Icon name={'searchMusic'} />
        </PresentComponent>
        <PresentComponent name={'searchThunder'}>
            <Icon name={'searchThunder'} />
        </PresentComponent>
        <PresentComponent name={'stamp'}>
            <Icon name={'stamp'} />
        </PresentComponent>
        <PresentComponent name={'thunder'}>
            <Icon name={'thunder'} />
        </PresentComponent>
        <PresentComponent name={'timeOut'}>
            <Icon name={'timeOut'} />
        </PresentComponent>
        <PresentComponent name={'training'}>
            <Icon name={'training'} />
        </PresentComponent>
        <PresentComponent name={'trophy'}>
            <Icon name={'trophy'} />
        </PresentComponent>
        <PresentComponent name={'user'}>
            <Icon name={'user'} />
        </PresentComponent>
        <PresentComponent name={'youtube'}>
            <Icon name={'youtube'} />
        </PresentComponent>
        <PresentComponent name={'ticTacToeArrow'}>
            <Icon name={'ticTacToeArrow'} />
        </PresentComponent>
        <PresentComponent name={'hourglass'}>
            <Icon name={'hourglass'} />
        </PresentComponent>
        <PresentComponent name={'checkedList'}>
            <Icon name={'checkedList'} />
        </PresentComponent>
    </Stack>
  </Story>
</Preview>

# Icon with color

Icon with color.

<Preview>
  <Story name="Icon with color">
    <Stack>
       <PresentComponent name={'primary'}>
        <Icon name={'add'} color={'primary'} />
       </PresentComponent>
       <PresentComponent name={'secondary'}>
         <Icon name={'add'} color={'secondary'} />
       </PresentComponent>
       <PresentComponent name={'success'}>
         <Icon name={'add'} color={'success'} />
       </PresentComponent>
       <PresentComponent name={'error'}>
         <Icon name={'add'} color={'error'} />
       </PresentComponent>
       <PresentComponent name={'info'}>
         <Icon name={'add'} color={'info'} />
       </PresentComponent>
       <PresentComponent name={'warning'}>
         <Icon name={'add'} color={'warning'} />
       </PresentComponent>
       <PresentComponent name={'branded1'}>
         <Icon name={'add'} color={'branded1'} />
       </PresentComponent>
       <PresentComponent name={'branded2'}>
         <Icon name={'add'} color={'branded2'} />
       </PresentComponent>
    </Stack>
  </Story>
</Preview>


# Icon with size

Icon with size.

<Preview>
  <Story name="Icon with size">
    <Stack>
       <PresentComponent name={'default'}>
         <Icon name={'add'} />
       </PresentComponent>
       <PresentComponent name={'20'}>
         <Icon name={'add'} size={20} />
       </PresentComponent>
       <PresentComponent name={'46'}>
         <Icon name={'add'} size={46} />
       </PresentComponent>
    </Stack>
  </Story>
</Preview>

import { Meta, Preview, Props, Story } from '@storybook/addon-docs';
import { number, text, withKnobs } from '@storybook/addon-knobs';
import Icon from './Icon';
import Stack from '../storyUtils/Stack';
import IconographyShowcase from '../storyUtils/IconographyShowcase';
import { FIGMA_URL } from '../../utils/common';
import Overview from '../../storybook/Overview';
import { getIconSelectorKnob } from '../../utils/stories';

<Meta
  title="Updated Components/Icon"
  component={Icon}
  parameters={{
    design: [
      {
        type: 'figma',
        name: 'Icons',
        url: `${FIGMA_URL}?node-id=99%3A3192`,
      },
    ],
  }}
/>

<SectionHeader title={'Icon'} />

- [Overview](#overview)
- [Props](#props)
- [Collection](#collection)
- [Variants](#variants)

## Overview

<Story
  name="Overview"
  parameters={{
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
>
  <Overview>
    A component that includes a collection of icons that can be either decorative or interactive.
  </Overview>
</Story>

## Props

<Props of={Icon} />

## Collection

<Preview>
  <Story name="Collection">
    <IconographyShowcase />
  </Story>
</Preview>

<SubsectionHeader title="Variants" />

### Interactive Icon

To make the icon interactive, provide the `onClick` prop. The `hasHover` prop defaults to `true` if the `onClick` prop is provided.

<Preview>
  <Story name="Interactive Icon">
    <Stack>
      <Icon name="moreOptions" onClick={() => console.log('click')} size={12} />
    </Stack>
  </Story>
</Preview>

### Playground

<Preview>
  <Story name="Playground" parameters={{ decorators: [withKnobs] }}>
    <Stack isVertical>
      <Icon
        name={getIconSelectorKnob('name', 'informational')}
        size={number('size', undefined)}
        color={text('color', undefined)}
      />
    </Stack>
  </Story>
</Preview>

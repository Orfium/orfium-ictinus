import { Meta, Canvas, ArgTypes, Story } from '@storybook/blocks';
import Icon from './Icon';
import * as IconStories from './Icon.stories';

<Meta of={IconStories} />

<SectionHeader title={'Icon'} />

- [Overview](#overview)
- [Props](#props)
- [Collection](#collection)
- [Variants](#variants)

## Overview

A component that includes a collection of icons that can be either decorative or interactive.

## Props

<ArgTypes of={Icon} />

## Collection

<Canvas of={IconStories.Collection} />

<SubsectionHeader title="Variants" />

### Interactive Icon

To make the icon interactive, provide the `onClick` prop. The `hasHover` prop defaults to `true` if the `onClick` prop is provided.

<Canvas of={IconStories.InteractiveIcon} />

### Playground

<Canvas of={IconStories.Playground} />

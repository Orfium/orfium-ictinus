import { Meta, Canvas, ArgTypes } from '@storybook/blocks';
import { withKnobs, boolean, text } from '@storybook/addon-knobs';
import CheckBox from './CheckBox';
import Stack from '../storyUtils/Stack';
import { FIGMA_URL } from '../../utils/common';
import * as CheckBoxStories from './CheckBox.stories';

<Meta of={CheckBoxStories} />

# CheckBox

A universal CheckBox component that the input component for any forms.

## Usage

```
import { CheckBox } from '@orfium/ictinus';

<CheckBox />;
```

## Props

<ArgTypes of={CheckBox} />

# CheckBox

Regular CheckBox

<Canvas of={CheckBoxStories.CheckBoxStory} />

### CheckBox with Label

Regular CheckBox with label

<Canvas of={CheckBoxStories.CheckBoxWithLabel} />

<Canvas of={CheckBoxStories.CheckBoxWithProps} />

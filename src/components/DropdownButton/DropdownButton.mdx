import { Meta, Canvas, ArgTypes, Story } from '@storybook/blocks';
import DropdownButton from './DropdownButton';
import * as DropdownButtonStories from './DropdownButton.stories';
import Link from '../../storybook/Link';

<Meta of={DropdownButtonStories} />

<SectionHeader
  title={'DropdownButton'}
  sections={[
    { title: 'Overview', href: '#overview' },
    { title: 'Props', href: '#props' },
    { title: 'Usage', href: '#usage' },
    { title: 'Variants', href: '#variants' },
  ]}
/>

## Overview

A DropdownButton is a dual-function menu button that comes with a CTA and a dropdown with actions

## Props

<ArgTypes of={DropdownButton} />

## Usage

<UsageGuidelines
  guidelines={[
    'Split button contains two actions: One primary action and a list of secondary actions.',
    'Try limiting the overall number of choices within the menu to less than four.',
    'Order the items within the menu by popularity for a small number of items, or alphabetically for a larger number of items.',
  ]}
/>

<Tip>
  If the actions in the menu are not related to each other, consider using a{' '}
  <Link href="../?path=/docs/original-components-menu--overview">Menu</Link> component.
</Tip>

<SubsectionHeader title="Variants" />

### Button with DropdownButton

There are 2 Button with DropdownButton Sizes, `normal` and `compact`.

<Canvas of={DropdownButtonStories.ButtonWithDropdown} />

### IconButton with Dropdown

There are 2 IconButton with DropdownButton Sizes, `normal` and `compact`.

<Canvas of={DropdownButtonStories.IconButtonWithDropdown} />

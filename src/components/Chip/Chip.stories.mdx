import { Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { select, text } from '@storybook/addon-knobs';
import { colorShades, flatColors } from '../../theme/palette';
import { flexCenterVertical } from '../../theme/functions';
import Stack from '../storyUtils/Stack';
import ChipShowcase from '../storyUtils/ChipShowcase';
import Chip from './Chip';

<Meta title="Design System/Chip" component={Chip} />

# Card

A compact component that will be used in inner DS component but outside DS as well

# Usage

```js
import { Chip } from '@orfium/ictinus';
<Chip styleType='outlined' fill='red'>
  Label
</Chip>;
```

# Props

<Props of={Chip} />

<!-- # Chip Styles

Chip Styles

<Preview>
  <Story name="Chip Styles">
    <Stack>
        <Chip styleType='filled'
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Filled Style
        </Chip>
        <Chip styleType='outlined'
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Outlined Style
        </Chip>
    </Stack>
  </Story>
</Preview> -->

<!-- # Chip Sizes

Chip Sizes

<Preview>
  <Story name="Chip Sizes">
    <Stack>
        <Chip styleType={select('styleType', ['filled', 'outlined'], 'filled')} size='md'
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Medium Size
        </Chip>
        <Chip styleType={select('styleType', ['filled', 'outlined'], 'filled')} size='sm'
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Small Size
        </Chip>
    </Stack>
  </Story>
</Preview> -->

# Read-Only Chips

Read-Only Chips

<Preview>
  <Story name="Read-Only Chips">
    <Stack>
        <Chip
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Label
        </Chip>
        <Chip 
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
          thumbnail={{src: '', name: 'KN'}}
        >
        Label
        </Chip>
        <Chip 
          thumbnail={{src: 'https://mui.com/static/images/avatar/1.jpg', name: 'J'}}
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Label
        </Chip>
    </Stack>
  </Story>
</Preview>


# Chip with Icons

Chip with Icons
<Preview>
  <Story name="Chip with Icons">
    <Stack>
      <ChipShowcase
        fill={select('fill', flatColors.concat(undefined), flatColors[0])}
      >
        <div>{text('text', 'Label')}</div>
      </ChipShowcase>
    </Stack>
  </Story>
</Preview>

# Dynamic Chip Props

Dynamic Chip Props

<Preview>
  <Story name="Dynamic Chip Props">
    <Stack>
      <Chip
        fill={select('fill', flatColors.concat(undefined), flatColors[0])} 
      >
        <div>{text('text', 'change me!')}</div>
      </Chip>
    </Stack>
  </Story>
</Preview>

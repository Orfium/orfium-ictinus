import { Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { select, text } from '@storybook/addon-knobs';
import { colorShades, flatColors } from '../../theme/palette';
import Stack from '../storyUtils/Stack';
import ChipShowcase from '../storyUtils/ChipShowcase';
import Chip from './Chip';
import { FIGMA_URL } from '../../utils/common';

<Meta title="Design System/Chip" component={Chip} parameters={{
  design: [{
    type: 'figma',
    name: 'Chip',
    url: `${FIGMA_URL}?node-id=10062%3A105272`,
  },{
    type: 'figma',
    name: 'Anatomy',
    url: `${FIGMA_URL}?node-id=10038%3A104524`,
  },{
    type: 'figma',
    name: 'Doc: Chip',
    url: `${FIGMA_URL}?node-id=10037%3A121257`,
  },]
}} />

# Card

A compact component that will be used in inner DS component but outside DS as well

# Usage

```js
import { Chip } from '@orfium/ictinus';
<Chip styleType='outlined' size='sm' fill='red'>
  Label text
</Chip>;
```

# Props

<Props of={Chip} />

# Chip Styles

Chip Styles

<Preview>
  <Story name="Chip Styles">
    <Stack>
        <Chip styleType='filled' size={select('size', ['sm', 'md'], 'md')}
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Filled Style
        </Chip>
        <Chip styleType='outlined' size={select('size', ['sm', 'md'], 'md')}
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Outlined Style
        </Chip>
    </Stack>
  </Story>
</Preview>

# Chip Sizes

Chip Sizes

<Preview>
  <Story name="Chip Sizes">
    <Stack>
        <Chip styleType={select('styleType', ['filled', 'outlined'], 'filled')} size='md'
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Medium Size
        </Chip>
        <Chip styleType={select('styleType', ['filled', 'outlined'], 'filled')} size='sm'
          fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
        >
        Small Size
        </Chip>
    </Stack>
  </Story>
</Preview>


# Chip with Icons

Chip with Icons
<Preview>
  <Story name="Chip with Icons">
    <Stack>
      <ChipShowcase styleType={select('styleType', ['filled', 'outlined'], 'filled')} size={select('size', ['sm', 'md'], 'md')}
        fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
      >
        <div>{text('text', 'Label Text')}</div>
      </ChipShowcase>
    </Stack>
  </Story>
</Preview>

# Dynamic Chip Props

Dynamic Chip Props

<Preview>
  <Story name="Dynamic Chip Props">
    <Stack>
      <Chip styleType={select('styleType', ['filled', 'outlined'], 'filled')} size={select('size', ['sm', 'md'], 'md')}
        fill={select('fill', flatColors.concat(undefined), flatColors[0])} shade={select('shade', colorShades.concat(undefined), colorShades[0])}
      >
        <div>{text('text', 'change me!')}</div>
      </Chip>
    </Stack>
  </Story>
</Preview>

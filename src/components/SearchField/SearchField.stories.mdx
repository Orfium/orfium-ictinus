import { Meta, Preview, Props, Story } from '@storybook/addon-docs';
import Stack from '../storyUtils/Stack';
import SearchField from './SearchField';
import SearchFieldShowcase from './SearchFieldShowcase';
import { FIGMA_URL } from '../../utils/common';

<Meta
  title="Original Components/SearchField"
  component={SearchField}
  parameters={{
    design: [
      {
        type: 'figma',
        name: 'Anatomy',
        url: `${FIGMA_URL}?node-id=10231%3A104301`,
      },
      {
        type: 'figma',
        name: 'Rules',
        url: `${FIGMA_URL}?node-id=10241%3A104324`,
      },
      {
        type: 'figma',
        name: 'Search field',
        url: `${FIGMA_URL}?node-id=10283%3A104365`,
      },
    ],
  }}
/>

# SearchField

A Search field component based on the TextInputBase's styles.

## Usage

```js
import { SearchField } from '@orfium/ictinus';

<SearchField onInput={console.log} />;
```

## Props

<Props of={SearchField} />

# Search Field

Search Field

<Preview>
  <Story name="Search Field">
    <SearchFieldShowcase />
  </Story>
</Preview>

# Search Field disabled

Disabled Search Field

<Preview>
  <Story name="Search Field disabled">
    <SearchFieldShowcase isDisabled />
  </Story>
</Preview>

# Search Field with placeholder

Search Field with placeholder

<Preview>
  <Story name="Search Field with placeholder">
    <SearchFieldShowcase placeholder={'Placeholder'} />
  </Story>
</Preview>

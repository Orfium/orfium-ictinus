import { Meta, Preview, Props, Story } from '@storybook/addon-docs';
import Stack from '../storyUtils/Stack';
import SearchField from './SearchField';

<Meta title="Design System/SearchField" component={SearchField} />

# TextField

A universal SearchField component that the input component for any forms.

# Usage

```js
import { SearchField } from '@orfium/ictinus';

<SearchField onInput={console.log} />;
```

# Props

<Props of={SearchField} />

# Text Field Sizes

Regular SearchField and available sizes

<Preview>
  <Story name="Text Field Sizes">
    <Stack>
      <SearchField onInput={console.log} size={'md'} onClear={() => void 0} />
      <SearchField onInput={console.log} size={'sm'} onClear={() => void 0} />
    </Stack>
  </Story>
</Preview>

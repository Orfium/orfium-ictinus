import { Meta, Story, Preview, Props } from '@storybook/addon-docs';
import TopAppBar from './TopAppBar';
import Stack from '../storyUtils/Stack';
import TextField from '../TextField';
import TopAppBarShowcase from '../storyUtils/TopAppBarShowcase';
import { boolean } from '@storybook/addon-knobs';
import UserMenu from './components/UserMenu/UserMenu';
import { FIGMA_URL } from '../../utils/common';

<Meta
  title="Design System/TopNavBar"
  component={TopAppBar}
  parameters={{
    design: [
      {
        type: 'figma',
        url: `${FIGMA_URL}?node-id=1213%3A74735`,
      },
    ],
  }}
/>

# TopAppBar

A universal TopAppBar component.

## Usage

```js
import { TopNavBar } from '@orfium/ictinus';

<TopNavBar
  isDark={false}
  logoIcon={<Icon />}
  onMenuIconClick={() => {}}
  userMenu={{
    items: ['item1', 'item2'],
    userAvatar: { letter: 'JD', src: 'http://lorem.com/john-doe-profile.jpg' },
    userName: 'John Doe',
    onSelect: () => {},
  }}
  additionalTools={<div>Extra Information</div>}
  onSearchHandler={() => {}}
/>;
```

## Props

<Props of={TopAppBar} />

## UserMenu Props

<Props of={UserMenu} />

# TopAppBar with Logo Placeholder

<Preview>
  <Story name="with Logo Placeholder">
    <div style={{ padding: 10 }}>
      <TopAppBarShowcase />
    </div>
  </Story>
</Preview>

# TopAppBar with additionalTools

<Preview>
  <Story name="with Additional Tools">
    <div style={{ padding: 10 }}>
      <TopAppBarShowcase hasAdditionalTools={true} />
    </div>
  </Story>
</Preview>

# TopAppBar Dark Theme

<Preview>
  <Story name="with Dark theme enabled">
    <div style={{ padding: 10 }}>
      <TopAppBarShowcase isDark={true} />
    </div>
  </Story>
</Preview>

# TopAppBar Playground

TopAppBar Playground

<Preview>
  <Story name="playground">
    <div style={{ padding: 10 }}>
      <TopAppBarShowcase
        isDark={boolean('isDark', false)}
        isSearchDisabled={boolean('disabled search', false)}
        hasSearchHandler={boolean('hasSearchHandler', false)}
        hasAdditionalTools={boolean('hasAdditionalTools', false)}
        hasLogo={boolean('hasLogo', false)}
      />
    </div>
  </Story>
</Preview>

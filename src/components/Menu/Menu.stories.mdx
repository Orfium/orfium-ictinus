import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean, array, select, text } from '@storybook/addon-knobs';
import Menu from '../Menu';
import MenuShowcase from '../storyUtils/MenuShowcase';

<Meta title="Design System|Menu" component={Menu} />

# Menu

A universal Menu component that is a clickable element that can hold many forms.

# Usage

```js
import { Menu } from 'orfium-design-system';

<Menu onClick={() => {}})}>
  Click Me
</Menu>
```

# Props

<Props of={Menu} />

# Icon Menu Sizes

Menu sizes are 3 `sm, md, lg` and indicates the acronym of the size. Default is medium size

<Preview>
  <Story name="Icon Menu Sizes">
    <MenuShowcase />
  </Story>
</Preview>

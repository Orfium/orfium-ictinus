import { Meta, Canvas, ArgTypes, Story } from '@storybook/blocks';
import Menu from './Menu';
import * as MenuStories from './Menu.stories';

<Meta of={MenuStories} />

<SectionHeader title={'Menu'} />

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

A menu displays a list of choices on a temporary surface when the user interacts with its trigger element. A menu can take children as custom component or any `ListItem` from `List` by defining `parentType={'Menu'}` on the `ListItem`

## Props

<ArgTypes of={Menu} />

## Usage

<UsageGuidelines
  guidelines={['Use menu for list of selections that can be accessible via arrows']}
/>

### Menu different triggers

A menu can be triggered by different elements

<Canvas of={MenuStories.MenuTriggers} />

### Menu different row sizes

A menu can have different row sizes as `List` does

<Canvas of={MenuStories.MenuComponents} />

### Menu with divider

A menu can have specific MenuItemDivider

<Tip>To style the divider and bypass the default style include an `sx` prop</Tip>

<Canvas of={MenuStories.MenuItemDividerStory} />

### Menu with disabled items

A menu can have specific disabled items

<Canvas of={MenuStories.MenuDisabledKeys} />

import { Meta, Canvas, ArgTypes, Story, Primary } from '@storybook/blocks';
import * as InlineAlertStories from './InlineAlert.stories';
import { InlineAlert } from './InlineAlert';

<Meta of={InlineAlertStories} />

<SectionHeader
  title="Inline Alert"
  sections={[
    { title: 'Overview', href: '#overview' },
    { title: 'Props', href: '#props' },
    { title: 'Usage', href: '#usage' },
    { title: 'Variants', href: '#variants' },
  ]}
/>

## Overview

A message from the system to the user that offers contextual and timely information pertaining to the user's current task.

<Primary />

## Props

<ArgTypes of={InlineAlert} />

## Usage

<Tip>
  The InlineAlert component uses CSS Container Queries for responsive design. To enable this feature
  wrap the `InlineAlert` in a parent element and set the `container-type` CSS property on this
  parent element.
</Tip>

<UsageGuidelines
  guidelines={[
    'To provide contextual information to the user pertaining to their current task.',
    'In conjunction with form validation - When you need to learn about a feature or information that is relevant to your current activity.',
    'When you need assistance in fixing errors, often in conjunction with form validation.',
  ]}
  policies={[
    'If you need to deliver a global message from the admin to all users (e.g. maintenance update), use a broadcast message instead.',
    `If you are immediately confirming a user's action while they remain in the same view, use a toast instead.`,
    'If you are promoting, soliciting feedback, or informing the user about a feature, use a banner instead.',
  ]}
/>

<SubsectionHeader title="Variants" />

### Neutral

<Canvas of={InlineAlertStories.Neutral} />

### Informational

<Canvas of={InlineAlertStories.Informational} />

### Error

<Canvas of={InlineAlertStories.Error} />

### Warning

<Canvas of={InlineAlertStories.Warning} />

### Success

<Canvas of={InlineAlertStories.Success} />

### Playground

<Canvas of={InlineAlertStories.Playground} />

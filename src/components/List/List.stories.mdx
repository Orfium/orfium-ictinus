import { Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { select, number, boolean } from '@storybook/addon-knobs';

import ListShowcase from '../storyUtils/ListShowcase';
import List from './List';
import { FIGMA_URL } from '../../utils/common';

<Meta
  title="Design System/List"
  component={List}
  parameters={{
    design: {
      type: 'figma',
      url: `${FIGMA_URL}?node-id=708%3A2829`,
    },
  }}
/>

# List

A universal List component with different types.

# Usage

```js
import { List } from '@orfium/ictinus';

<List />;
```

# Props

<Props of={List} />

# List

List component.

<Preview>
  <Story name="List">
    <ListShowcase
      itemsCount={number('itemsCount', 300)}
      rowSize={select('rowSize', ['small', 'normal'], 'normal')}
      width={number('width', 300)}
      height={number('height', 300)}
      isVirtualized={boolean('isVirtualized', false)}
    />
  </Story>
</Preview>

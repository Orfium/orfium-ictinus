import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import Slider from './Slider';
import Stack from '../storyUtils/Stack';
import SwitchShowcase from '../storyUtils/SwitchShowcase';
import { boolean, select, withKnobs, text } from '@storybook/addon-knobs';
import SliderShowcase from '../storyUtils/SliderShowcase/SliderShowcase';

<Meta title="Design System/Slider" component={Slider} />

# Slider

An interactive Slider component meant to serve as an alternative way of toggling between states.

## Usage

```js
import { Slider } from '@orfium/ictinus';

<Slider />;
```

## Props

<ArgsTable of={Slider} />

### Range Slider

<Canvas>
  <Story name="Range Slider (DontTest)" parameters={{ decorators: [withKnobs] }}>
    <SliderShowcase isSelector={false} isDisabled={boolean('isDisabled', false)} />
  </Story>
</Canvas>

### Range Slider with Increments

<Canvas>
  <Story name="Range Slider with Increments (DontTest)" parameters={{ decorators: [withKnobs] }}>
    <SliderShowcase
      isSelector={false}
      isDisabled={boolean('isDisabled', false)}
      hasIncrements={true}
    />
  </Story>
</Canvas>

### Selector Slider

<Canvas>
  <Story name="Selector Slider (DontTest)" parameters={{ decorators: [withKnobs] }}>
    <SliderShowcase isSelector={true} isDisabled={boolean('isDisabled', false)} />
  </Story>
</Canvas>

### Selector Slider with Increments

<Canvas>
  <Story name="Selector Slider with Increments (DontTest)" parameters={{ decorators: [withKnobs] }}>
    <SliderShowcase
      isDisabled={boolean('isDisabled', false)}
      isSelector={true}
      hasIncrements={true}
    />
  </Story>
</Canvas>

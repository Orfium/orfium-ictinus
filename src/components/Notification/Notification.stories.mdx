import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, select, boolean } from '@storybook/addon-knobs';
import Notification from '../Notification';
import BannersContainer from '../Notification/subcomponents/BannersContainer';
import PresentComponent from '../storyUtils/PresentComponent';

<Meta title="Design System/Notification" component={Notification} />

# Notification

# Usage

```js
import { Notification } from '@orfium/ictinus';

<Notification />;
```

# Props

<Props of={Notification} />

# Inline Notification

Inline Notification

<Preview>
  <Story name="Inline Notifiction" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={1024}>
      <Notification
        withFilling={boolean('withFilling', false)}
        withIcon={boolean('icon', true)}
        message={text('info', 'This is the info')}
        type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
        variant="inline"
        primaryCTALabel={text('primaryCTALabel', 'Action')}
        primaryCTA={() => console.log('action clicked')}
        closeCTA={() => console.log('close action clicked')}
      />
    </PresentComponent>
  </Story>
</Preview>

# Banner Notification

Banner Notification

<Preview>
  <Story name="Banner Notifiction" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={1024}>
      <BannersContainer
        top={boolean('top', false)}
        bottom={boolean('bottom', false)}
        left={boolean('left', true)}
        right={boolean('right', false)}
      >
        <Notification
          withFilling={boolean('withFilling', false)}
          withIcon={boolean('icon', true)}
          title={text('title', 'This is the heading:')}
          message={text('info', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          variant="banner"
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
        <Notification
          withFilling={boolean('withFilling', false)}
          withIcon={boolean('icon', true)}
          title={text('title', 'This is the heading:')}
          message={text('info', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          variant="banner"
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
        <Notification
          withFilling={boolean('withFilling', false)}
          withIcon={boolean('icon', true)}
          title={text('title', 'This is the heading:')}
          message={text('info', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          variant="banner"
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
      </BannersContainer>
    </PresentComponent>
  </Story>
</Preview>

# Playground Notification

Playground Notification

<Preview>
  <Story name="Playground Notifiction" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={600}>
      <Notification
        withFilling={boolean('withFilling', false)}
        withIcon={boolean('icon', true)}
        title={text('title', 'This is the heading:')}
        message={text('info', 'This is the info')}
        type={select('type', ['success', 'error', 'info', 'wanring'], 'success')}
        variant={select('variant', ['inline', 'banner', 'modal', 'toast'], 'inline')}
        primaryCTALabel={text('primaryCTALabel', 'Action')}
        primaryCTA={() => console.log('primary action clicked')}
        onCloseCTA={() => console.log('close action clicked')}
      />
    </PresentComponent>
  </Story>
</Preview>

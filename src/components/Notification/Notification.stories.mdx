import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, select, boolean } from '@storybook/addon-knobs';
import Notification from '../Notification';
import Toast from '../Notification/Toast';
import NotificationVisual from '../Notification/NotificationVisual';
import NotificationsContainer from '../Notification/subcomponents/NotificationsContainer';
import CardNotification from '../Notification/CardNotification';
import PresentComponent from '../storyUtils/PresentComponent';
import Stack from '../storyUtils/Stack';

<Meta title="Design System/Notification" component={Notification} />

# Notification

# Usage

```js
import { Notification } from '@orfium/ictinus';

<Notification />;
```

# Props

<Props of={Notification} />

# Inline Notification

Inline Notification

<Preview>
  <Story name="Inline Notification" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <PresentComponent name="" width={768}>
        <Notification
          message={text('message', 'Informative Message')}
          variant="inline"
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
        />
      </PresentComponent>
      <PresentComponent name="" width={768}>
        <Notification
          message={text('message', 'Informative Message')}
          variant="inline"
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
      </PresentComponent>
      <PresentComponent name="" width={768}>
        <Notification
          message={text('message', 'Informative Message')}
          variant="inline"
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
        />
      </PresentComponent>
      <PresentComponent name="" width={768}>
        <Notification
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
          message={text('message', 'Informative Message')}
          variant="inline"
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          closeCTA={() => console.log('close action clicked')}
        />
      </PresentComponent>
    </Stack>
  </Story>
</Preview>

# Banner Notification

Banner Notification

<Preview>
  <Story name="Banner Notification" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={1024}>
      <NotificationsContainer
        position={select(
          'position',
          ['top-right', 'top-left', 'bottom-left', 'bottom-right'],
          'bottom-right'
        )}
      >
        <Notification
          withFilling={boolean('withFilling', false)}
          withIcon={boolean('icon', true)}
          title={text('title', 'This is the heading:')}
          message={text('message', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          variant="banner"
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
        <Notification
          withFilling={boolean('withFilling', false)}
          withIcon={boolean('icon', true)}
          title={text('title', 'This is the heading:')}
          message={text('message', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          variant="banner"
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
        <Notification
          withFilling={boolean('withFilling', false)}
          withIcon={boolean('icon', true)}
          title={text('title', 'This is the heading:')}
          message={text('message', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          variant="banner"
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
      </NotificationsContainer>
    </PresentComponent>
  </Story>
</Preview>

# Toast Notification

Toast Notification

<Preview>
  <Story name="Toast Notifiction" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={768}>
      <Stack>
        <Toast
          message="Branded2 colored Toast"
          type="branded2"
          closeCTA={() => console.log('close action clicked')}
        ></Toast>
        <Toast
          message={text('message', 'Informative Message')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          closeCTA={() => console.log('close action clicked')}
        >
          <NotificationVisual
            title={text('title', 'Message heading')}
            primaryCTALabel={text('primaryCTALabel', 'Primary CTA')}
            primaryCTA={() => console.log('primary action clicked')}
            secondaryCTALabel={text('secondaryCTALabel', 'Secondary CTA')}
            secondaryCTA={() => console.log('secondary action clicked')}
            description={text(
              'description',
              'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed viverra neque nec est porttitor'
            )}
          />
        </Toast>
        <Toast
          message="Default colored Toast"
          type="generic"
          closeCTA={() => console.log('close action clicked')}
        >
          <div>
            <h1>
              Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed viverra neque nec est
              porttitor
            </h1>
            <ul>
              <li>
                <h1>Lorem ipsum dolor sit amet</h1>
              </li>
              <li>
                <h1>consectetur adipiscing elit</h1>
              </li>
              <li>
                <h1>Sed viverra neque</h1>
              </li>
              <li>
                <h1>neque nec</h1>
              </li>
              <li>
                <h1>est porttitor</h1>
              </li>
              <li>
                <h1>dolor sit amet</h1>
              </li>
            </ul>
          </div>
        </Toast>
      </Stack>
    </PresentComponent>
  </Story>
</Preview>

# Playground Notification

Playground Notification

<Preview>
  <Story name="Playground Notification" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={768}>
      <Notification
        withIcon={boolean('withIcon', true)}
        withFilling={boolean('withFilling', false)}
        title={text('title', 'This is the heading:')}
        message={text('message', 'This is the info')}
        variant={select('variant', ['inline', 'banner', 'modal', 'toast'], 'inline')}
        type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
        primaryCTALabel={text('primaryCTALabel', 'Action')}
        primaryCTA={() => console.log('action clicked')}
        closeCTA={() => console.log('close action clicked')}
      />
    </PresentComponent>
  </Story>
</Preview>

# Card Notification

<Preview>
  <Story name="Card Notification" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={768}>
      <CardNotification
        message={text('message', 'This is the info')}
        type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
        primaryCTALabel={text('primaryCTALabel', 'Primary CTA')}
        primaryCTA={() => console.log('primary action clicked')}
        secondaryCTALabel={text('secondaryCTALabel', 'Secondary CTA')}
        secondaryCTA={() => console.log('secondary action clicked')}
        description={text(
          'description',
          'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed viverra neque nec est porttitor'
        )}
        closeCTA={() => console.log('close action clicked')}
      />
    </PresentComponent>
  </Story>
</Preview>

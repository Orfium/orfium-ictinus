import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, select, boolean } from '@storybook/addon-knobs';
import InlineNotification from '../Notification/InlineNotification';
import Banner from '../Notification/Banner';
import NotificationsContainer from '../Notification/NotificationsContainer';
import PresentComponent from '../storyUtils/PresentComponent';
import Stack from '../storyUtils/Stack';

<Meta title="Design System/Notification" component={InlineNotification} />

# Notification

# Inline Notification

### Props

<Props of={InlineNotification} />

### Usage

```js
import { InlineNotification } from '@orfium/ictinus';

<InlineNotification
  withIcon
  withFilling
  message="Message"
  type="info"
  primaryCTALabel='Action'
  primaryCTA={() => void}
  closeCTA={() => void}
/>
```

<Preview>
  <Story name="Inline Notification" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <PresentComponent name="" width={768}>
        <InlineNotification
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
          message={text('message', 'Informative Message')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
        />
      </PresentComponent>
      <PresentComponent name="" width={768}>
        <InlineNotification
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
          message={text('message', 'Informative Message')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
      </PresentComponent>
      <PresentComponent name="" width={768}>
        <InlineNotification
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
          message={text('message', 'Informative Message')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
        />
      </PresentComponent>
      <PresentComponent name="" width={768}>
        <InlineNotification
          withIcon={boolean('withIcon', true)}
          withFilling={boolean('withFilling', false)}
          message={text('message', 'Informative Message')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          closeCTA={() => console.log('close action clicked')}
        />
      </PresentComponent>
    </Stack>
  </Story>
</Preview>

# Banner Notification

### Props

<Props of={Banner} />

### Usage

```js
import { NotificationsContainer, Banner } from '@orfium/ictinus';

<NotificationsContainer position='top-left'>
  <Banner
    withIcon
    withFilling
    title="Title"
    message="Message"
    type="info"
    primaryCTALabel='Action'
    primaryCTA={() => void}
    closeCTA={() => void}
  />
  // add more Banners components here if needed
</NotificationsContainer>
```

<Preview>
  <Story name="Banner Notification" parameters={{ decorators: [withKnobs] }}>
    <PresentComponent name="" width={1024}>
      <NotificationsContainer
        position={select(
          'position',
          ['top-right', 'top-left', 'bottom-left', 'bottom-right'],
          'bottom-right'
        )}
      >
        <Banner
          withIcon={boolean('icon', true)}
          withFilling={boolean('withFilling', false)}
          title={text('title', 'This is the heading:')}
          message={text('message', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
        <Banner
          withIcon={boolean('icon', true)}
          withFilling={boolean('withFilling', false)}
          title={text('title', 'This is the heading:')}
          message={text('message', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
        <Banner
          withIcon={boolean('icon', true)}
          withFilling={boolean('withFilling', false)}
          title={text('title', 'This is the heading:')}
          message={text('message', 'This is the info')}
          type={select('type', ['success', 'error', 'info', 'warning'], 'success')}
          primaryCTALabel={text('primaryCTALabel', 'Action')}
          primaryCTA={() => console.log('action clicked')}
          closeCTA={() => console.log('close action clicked')}
        />
      </NotificationsContainer>
    </PresentComponent>
  </Story>
</Preview>

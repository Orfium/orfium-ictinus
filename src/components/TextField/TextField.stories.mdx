import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean, array, select, text } from '@storybook/addon-knobs';
import TextField from './TextField';
import Stack from '../storyUtils/Stack';
import TextFieldShowcase from '../storyUtils/TextFieldShowcase'; import { colorShades, flatColors } from '../../theme/palette'; import Icon
  from '../Icon'; import iconSelector from '../Icon/assets/iconSelector';

<Meta title="Design System/TextField" component={TextField} />

# TextField

A universal TextField component that the input component for any forms.

# Usage

```js
import { TextField } from '@orfium/ictinus';

<TextField label={'label'} />;
```

# Props

<Props of={TextField} />

# Text Field with Label

Regular TextField with label

<Preview>
  <Story name="Text Field with Label">
    <Stack>
      <TextField
        label={'Label'}
        lean={boolean('lean version', false)}
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
    </Stack>
  </Story>
</Preview>

# Text Field without Label

Regular TextField without label

<Preview>
  <Story name="Text Field without Label">
    <Stack>
      <TextField
        lean={boolean('lean version', false)}
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
    </Stack>
  </Story>
</Preview>

# Text Field with Placeholder

Regular TextField with placeholder

<Preview>
  <Story name="Text Field with Placeholder">
    <Stack>
      <TextField
        lean={boolean('lean version', false)}
        placeholder="placeholder"
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
    </Stack>
  </Story>
</Preview>

# Text Field with Placeholder, icon and size

Regular TextField with placeholder, icon and size

<Preview>
  <Story name="Text Field with Placeholder, icon and size">
    <Stack>
      <TextField
        size={select('size', ['md','sm'],'sm')}
        lean={boolean('lean version', false)}
        leftIcon={
          <img src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
        placeholder="placeholder"
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
      <TextField
        size={select('size', ['md','sm'], 'sm')}
        lean={boolean('lean version', false)}
        rightIcon={
          <img style={{background: 'red'}} src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
        placeholder="placeholder"
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
      <TextField
        size={select('size', ['md','sm'], 'sm')}
        lean={boolean('lean version', false)}
        label={'Label'}
        leftIcon={
          <img style={{background: 'red'}} src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
    </Stack>
  </Story>
</Preview>


# Text Field with color type and icon picker

Regular TextField with color type and icon picker

<Preview>
  <Story name="Text Field with color type and icon picker">
    <Stack>
      <TextField
        fill={select('fill', flatColors, 'lightGray')}
        fillShade={select('fillShade', colorShades, 100)}
        size={select('size', ['md','sm'], 'sm')}
        lean={boolean('lean version', false)}
        label={'Label'}
        LeftIconWithSwatches={({colorConfig}) =>
          <Icon name={select('name',Object.keys(iconSelector),'search')} colorConfig={colorConfig} />}
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
    </Stack>
  </Story>
</Preview>

# TextField disabled with label
Regular TextField disabled with label

<Preview>
  <Story name="TextField disabled with label">
    <Stack>
      <TextField
        disabled
        label={'Label'}
        lean={boolean('lean version', false)}
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
      />
    </Stack>
  </Story>
</Preview>

# TextField with Label and Error Handling Options

Regular TextField with Label and Error Handling Options

<Preview>
  <Story name="TextField with label and error handling options" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <TextFieldShowcase
        errorMsg={text('custom error message', '')}
        label={'Label'}
        lean={boolean('lean version', false)}
        status={select('status', ['success', 'error'], 'error')}
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'filled')}
        withErrorMsg={boolean('with error message', false)}
        withIndicator={boolean('with indicator', false)}
      />
    </Stack>
  </Story>
</Preview>

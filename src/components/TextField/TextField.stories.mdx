import { Meta, Preview, Props, Story } from '@storybook/addon-docs';
import { boolean, select, text, withKnobs } from '@storybook/addon-knobs';
import TextField from './TextField';
import Stack from '../storyUtils/Stack';
import Icon from '../Icon';
import iconSelector from '../Icon/assets/iconSelector';
import Loader from '../Loader';
import TextFieldShowcase from '../../components/storyUtils/TextFieldShowcase/TextFieldShowcase.tsx';
import { FIGMA_URL } from '../../utils/common';

<Meta
  title="Design System/TextField"
  component={TextField}
  parameters={{
    design: [
      {
        type: 'figma',
        url: `${FIGMA_URL}?node-id=10019%3A116505`,
      },
    ],
  }}
/>

# TextField

A universal TextField component that can be used as the <input/> in any form.

## Usage

```js
import { TextField } from '@orfium/ictinus';

<TextField label={'label'} />;
```

## Props

<Props of={TextField} />

# Text Field with and without Label

Regular TextField with label options. Label that will float, placeholder and no placeholder

<Preview>
  <Story name="Text Field with and without Label">
    <Stack>
      <TextField label={'Label'} />
      <TextField placeholder={'Placeholder'} />
      <TextField />
    </Stack>
  </Story>
</Preview>

# Text Field Icons

TextField with icon options right and left of the text

<Preview>
  <Story name="Text Field with Icons">
    <Stack>
      <TextField
        label={'Label'}
        prefix={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
      />
      <TextField
        label={'Label'}
        suffix={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
      />
      <TextField
        label={'Label'}
        prefix={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
        suffix={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
      />
      <TextField label={'Label'} suffix={<Loader type={'dots'} />} />
    </Stack>
  </Story>
</Preview>

# TextField with prefix/suffix

Regular TextField with a prefix or a suffix

<Preview>
  <Story name="TextField with prefix/suffix">
    <Stack>
      <TextField prefix={<div style={{ color: 'gray' }}>$</div>} />
      <TextField suffix={<div style={{ color: 'gray' }}>%</div>} />
      <TextField
        prefix={<div style={{ color: 'gray' }}>$</div>}
        suffix={<div style={{ color: 'gray' }}>%</div>}
      />
    </Stack>
  </Story>
</Preview>

# TextField disabled with label

Regular TextField disabled with label

<Preview>
  <Story name="TextField disabled with label">
    <Stack>
      <TextField isDisabled label={'Label'} />
    </Stack>
  </Story>
</Preview>

# TextField with Label and Statuses

Regular TextField with Label and Statuses.

<Preview>
  <Story name="TextField with label and statuses" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <TextField
        label={'Label'}
        status={{
          type: 'normal',
          hintMessage: text('Custom Hint Message', 'Hint in Text Field'),
        }}
      />
      <TextField
        label={'Label'}
        status={{
          type: 'error',
          hintMessage: text('Custom Error Message', 'Error in Text Field'),
        }}
      />
      <TextField
        label={'Label'}
        status={{
          type: 'read-only',
        }}
      />
    </Stack>
  </Story>
</Preview>

# Multi TextField

<Preview>
  <Story name="Multi TextField">
    <div>
      <TextField
        isMulti
        multiValues={['Orange', 'Citrus']}
        label={text('Label', 'Label')}
        locked={boolean('Locked', false)}
        disabled={boolean('Disabled', false)}
        hintMsg={text('Custom message', 'Press Enter to create multiple values')}
        status={select('Status', ['hint', 'error', 'normal'], 'hint')}
        maxMultiValues={text('maxMultiValues', 10)}
        sx={{
          textField: { width: '100%' },
        }}
      />
    </div>
    <TextFieldShowcase />
  </Story>
</Preview>

# Text Field with KNOBS

Regular Text Field with KNOBS

<Preview>
  <Story name="Text Field with KNOBS" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <TextField
        label={text('Label', 'Label')}
        isDisabled={boolean('isDisabled', false)}
        prefix={select('Prefix icon name', ['', ...Object.keys(iconSelector)], 'calendarEmpty')}
        suffix={select('Suffix icon name', ['', ...Object.keys(iconSelector)], 'info')}
        status={{
          type: select('Status', ['error', 'normal', 'read-only'], 'normal'),
          hintMessage: text('Hint/Error message', 'Message in Text Field'),
        }}
      />
    </Stack>
  </Story>
</Preview>

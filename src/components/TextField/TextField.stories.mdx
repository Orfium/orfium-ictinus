import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean, array, select, text } from '@storybook/addon-knobs';
import TextField from './TextField';
import Stack from '../storyUtils/Stack';
import TextFieldShowcase from '../storyUtils/TextFieldShowcase';

<Meta title="Design System/TextField" component={TextField} />

# TextField

A universal TextField component that the input component for any forms.

# Usage

```js
import { TextField } from 'orfium-design-system';

<TextField label={'label'} />;
```

# Props

<Props of={TextField} />

# Text Field with Label

Regular TextField with label

<Preview>
  <Story name="Text Field with Label">
    <Stack>
      <TextField label={'Label'} />
    </Stack>
  </Story>
</Preview>

# Text Field without Label

Regular TextField without label

<Preview>
  <Story name="Text Field without Label">
    <Stack>
      <TextField />
    </Stack>
  </Story>
</Preview>

# Text Field with Placeholder

Regular TextField with placeholder

<Preview>
  <Story name="Text Field with Placeholder">
    <Stack>
      <TextField placeholder="placeholder" />
    </Stack>
  </Story>
</Preview>

# Text Field with Placeholder and icon

Regular TextField with placeholder and icon

<Preview>
  <Story name="Text Field with Placeholder and icon">
    <Stack>
      <TextField
        placeholder="placeholder"
        leftIcon={
          <img src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
      />
    </Stack>
  </Story>
</Preview>

# TextField disabled with label

Regular TextField disabled with label

<Preview>
  <Story name="TextField disabled with label">
    <Stack>
      <TextField label={'Label'} disabled />
    </Stack>
  </Story>
</Preview>

# TextField with label and error

Regular TextField with label and error

<Preview>
  <Story name="TextField with label and error">
    <Stack>
      <TextField label={'Label'} error />
    </Stack>
  </Story>
</Preview>

# TextField with label and indicator

Regular TextField with label and indicator

<Preview>
  <Story name="TextField with label and indicator" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <TextFieldShowcase label={'Label'} withIndicator status={select('status', ['success', 'error'], 'success')} />
    </Stack>
  </Story>
</Preview>

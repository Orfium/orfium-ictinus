import { Meta, Preview, Props, Story } from '@storybook/addon-docs';
import { boolean, select, text, withKnobs } from '@storybook/addon-knobs';
import TextField from './TextField';
import Stack from '../storyUtils/Stack';
import Icon from '../Icon';
import iconSelector from '../Icon/assets/iconSelector';
import Loader from '../Loader';
import { FIGMA_URL } from '../../utils/common';

<Meta
     title="Design System/TextField"
     component={TextField}
     parameters={{
            design: [{
                type: 'figma',
                url: `${FIGMA_URL}?node-id=10019%3A116505`,
            }],
        }} />

# TextField

A universal TextField component that can be used as the <input/> in any form.

# Usage

```js
import { TextField } from '@orfium/ictinus';

<TextField label={'label'} />;
```

# Props

<Props of={TextField} />

# Text Field Types

Regular TextField and the 3 available types - filled, outlined, elevated

<Preview>
  <Story name="Text Field Types">
    <Stack>
      <TextField label={'Label'} size={'md'} styleType={'filled'} />
      <TextField label={'Label'} size={'md'} styleType={'outlined'} />
      <TextField label={'Label'} size={'md'} styleType={'elevated'} />
    </Stack>
  </Story>
</Preview>

# Text Field Sizes

Regular TextField and available sizes

<Preview>
  <Story name="Text Field Sizes">
    <Stack>
      <TextField label={'Label'} size={'md'} styleType={'outlined'} />
      <TextField label={'Label'} size={'sm'} styleType={'outlined'} />
    </Stack>
  </Story>
</Preview>

# Text Field with and without Label

Regular TextField with label options. Label that will float, placeholder and no placeholder

<Preview>
  <Story name="Text Field with and without Label">
    <Stack>
      <TextField label={'Label'} size={'md'} styleType={'outlined'} />
      <TextField placeholder={'Placeholder'} size={'md'} styleType={'outlined'} />
      <TextField size={'md'} styleType={'outlined'} />
    </Stack>
  </Story>
</Preview>

# Text Field Icons

TextField with icon options right and left of the text

<Preview>
  <Story name="Text Field with Icons">
    <Stack>
      <TextField
        size={select('size', ['md', 'sm'], 'md')}
        label={'Label'}
        leftIcon={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'outlined')}
      />
      <TextField
        size={select('size', ['md', 'sm'], 'md')}
        label={'Label'}
        rightIcon={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'outlined')}
      />
      <TextField
        size={select('size', ['md', 'sm'], 'md')}
        label={'Label'}
        leftIcon={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
        rightIcon={
          <img
            style={{ background: 'red' }}
            src="https://brandmark.io/logo-rank/random/pepsi.png"
            width="16"
            height="16"
          />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'outlined')}
      />
      <TextField
        size={select('size', ['md', 'sm'], 'md')}
        label={'Label'}
        rightIcon={
          <Loader type={'dots'} />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'outlined')}
      />
    </Stack>
  </Story>
</Preview>

# TextField disabled with label

Regular TextField disabled with label

<Preview>
  <Story name="TextField disabled with label">
    <Stack>
      <TextField isDisabled label={'Label'} size={'md'} styleType={'filled'} />
      <TextField isDisabled label={'Label'} size={'md'} styleType={'outlined'} />
      <TextField isDisabled label={'Label'} size={'md'} styleType={'elevated'} />
    </Stack>
  </Story>
</Preview>

# TextField locked with label

Regular TextField locked with label. Locked state defined a locked text field but not disabled (style-wise)

<Preview>
  <Story name="TextField locked with label">
    <Stack>
      <Stack>
        <TextField isLocked label={'Label'} size={'md'} styleType={'filled'} />
        <TextField isLocked label={'Label'} size={'md'} styleType={'outlined'} />
        <TextField isLocked label={'Label'} size={'md'} styleType={'elevated'} />
      </Stack>
      <Stack>
        <TextField isLocked label={'Label'} size={'sm'} styleType={'filled'} />
        <TextField isLocked label={'Label'} size={'sm'} styleType={'outlined'} />
        <TextField isLocked label={'Label'} size={'sm'} styleType={'elevated'} />
      </Stack>
    </Stack>
  </Story>
</Preview>

# TextField with Label and Statuses

Regular TextField with Label and Statuses. Error and Hint are shown below for all 3 types filled, outlined, elevated

<Preview>
  <Story name="TextField with label and statuses" parameters={{ decorators: [withKnobs] }}>
    <Stack isVertical>
      <Stack>
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'error'}
          styleType={'outlined'}
        />
        <div style={{ width: '200px' }}>
          <TextField
            hintMsg={
              'Big Error Message for fixed width TextField to see icon alignment: Error Error Error Error Error Error Error Error Error Error Error'
            }
            label={'Label'}
            status={'error'}
            styleType={'outlined'}
          />
        </div>
      </Stack>
      <Stack>
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'hint'}
          styleType={'outlined'}
        />
        <div style={{ width: '200px' }}>
          <TextField
            hintMsg={
              'Big Hint Message for fixed width TextField to see icon alignment: Hint Hint Hint Hint Hint Hint Hint Hint Hint Hint'
            }
            label={'Label'}
            status={'hint'}
            styleType={'outlined'}
          />
        </div>
      </Stack>
    </Stack>
  </Story>
</Preview>

# Text Field with KNOBS

Regular Text Field with KNOBS

<Preview>
  <Story name="Text Field with KNOBS" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <TextField
        label={text('Label', 'Label')}
        size={select('Size', ['md', 'sm'], 'md')}
        isLean={boolean('Lean version', false)}
        isLocked={boolean('isLocked', false)}
        isDisabled={boolean('isDisabled', false)}
        leftIcon={select('LeftIcon', ['', ...Object.keys(iconSelector)], 'calendarEmpty')}
        rightIcon={select('RightIcon', ['', ...Object.keys(iconSelector)], 'info')}
        styleType={select('Style', ['filled', 'outlined', 'elevated'], 'outlined')}
        hintMsg={text('custom error message', 'Error in Text Field')}
        status={select('status', ['hint', 'error', 'normal'], 'normal')}
      />
      <TextField
        label={text('Label', 'Label')}
        size={select('Size', ['md', 'sm'], 'md')}
        isLean={boolean('Lean version', false)}
        isLocked={boolean('isLocked', false)}
        isDisabled={boolean('isDisabled', false)}
        leftIcon={
          <Icon
            name={select('LeftIcon', ['', ...Object.keys(iconSelector)], 'calendarEmpty')}
            size={24}
            color={'#f5781b'}
          />
        }
        rightIcon={
          <Icon
            name={select('RightIcon', ['', ...Object.keys(iconSelector)], 'info')}
            size={24}
            color={'#f5781b'}
          />
        }
        styleType={select('Style', ['filled', 'outlined', 'elevated'], 'outlined')}
        hintMsg={text('custom error message', 'Error in Text Field')}
        status={select('status', ['hint', 'error', 'normal'], 'normal')}
      />
    </Stack>
  </Story>
</Preview>

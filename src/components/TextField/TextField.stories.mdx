import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import { boolean, select, text, withKnobs } from '@storybook/addon-knobs';
import TextField from './TextField';
import Stack from '../storyUtils/Stack';
import Icon from '../Icon';
import iconSelector from '../Icon/assets/iconSelector';

<Meta title="Design System/TextField" component={TextField} />

# TextField

A universal TextField component that the input component for any forms.

# Usage

```js
import { TextField } from '@orfium/ictinus';

<TextField label={'label'} />;
```

# Props

<Props of={TextField} />

# Text Field Types

Regular TextField and the 3 available types - filled, outlined, elevated

<Preview>
  <Story name="Text Field Types">
    <Stack>
      <TextField
        label={'Label'}
        size={'md'}
        styleType={'filled'}
      />
      <TextField
        label={'Label'}
        size={'md'}
        styleType={'outlined'}
      />
      <TextField
        label={'Label'}
        size={'md'}
        styleType={'elevated'}
      />
    </Stack>
  </Story>
</Preview>

# Text Field Sizes

Regular TextField and available sizes

<Preview>
  <Story name="Text Field Sizes">
    <Stack>
      <TextField
        label={'Label'}
        size={'md'}
        styleType={'outlined'}
      />
      <TextField
        label={'Label'}
        size={'sm'}
        styleType={'outlined'}
      />
    </Stack>
  </Story>
</Preview>

# Text Field with and without Label

Regular TextField with label options. Label that will float, placeholder and no placeholder

<Preview>
  <Story name="Text Field with Label">
    <Stack>
      <TextField
        label={'Label'}
        size={'md'}
        styleType={'outlined'}
      />
      <TextField
        placeholder={'Label'}
        size={'md'}
        styleType={'outlined'}
      />
      <TextField
        size={'md'}
        styleType={'outlined'}
      />
    </Stack>
  </Story>
</Preview>


# Text Field Icons

TextField with icon options right and left of the text

<Preview>
  <Story name="Text Field with Placeholder, icon and size">
    <Stack>
      <TextField
        size={select('size', ['md','sm'], 'md')}
        label={'Label'}
        leftIcon={
          <img style={{background: 'red'}} src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'outlined')}
      />
      <TextField
        size={select('size', ['md','sm'], 'md')}
        label={'Label'}
        rightIcon={
          <img style={{background: 'red'}} src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'outlined')}
      />
      <TextField
        size={select('size', ['md','sm'], 'md')}
        label={'Label'}
        leftIcon={
          <img style={{background: 'red'}} src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
        rightIcon={
          <img style={{background: 'red'}} src="https://brandmark.io/logo-rank/random/pepsi.png" width="16" height="16" />
        }
        styleType={select('style', ['filled', 'outlined', 'elevated'], 'outlined')}
      />
    </Stack>
  </Story>
</Preview>

# TextField disabled with label
Regular TextField disabled with label

<Preview>
  <Story name="TextField disabled with label">
    <Stack>
      <TextField
        disabled
        label={'Label'}
        size={'md'}
        styleType={'filled'}
      />
      <TextField
        disabled
        label={'Label'}
        size={'md'}
        styleType={'outlined'}
      />
      <TextField
        disabled
        label={'Label'}
        size={'md'}
        styleType={'elevated'}
      />
    </Stack>
  </Story>
</Preview>

# TextField locked with label
Regular TextField locked with label. Locked state defined a locked text field but not disabled (style-wise)

<Preview>
  <Story name="TextField locked with label">
    <Stack>
      <Stack>
        <TextField
          locked
          label={'Label'}
          size={'md'}
          styleType={'filled'}
        />
        <TextField
          locked
          label={'Label'}
          size={'md'}
          styleType={'outlined'}
        />
        <TextField
          locked
          label={'Label'}
          size={'md'}
          styleType={'elevated'}
        />
      </Stack>
      <Stack>
        <TextField
          locked
          label={'Label'}
          size={'sm'}
          styleType={'filled'}
        />
        <TextField
          locked
          label={'Label'}
          size={'sm'}
          styleType={'outlined'}
        />
        <TextField
          locked
          label={'Label'}
          size={'sm'}
          styleType={'elevated'}
        />
      </Stack>
    </Stack>
  </Story>
</Preview>

# TextField with Label and Statuses

Regular TextField with Label and Statuses. Error and Hint are shown below for all 3 types filled, outlined, elevated

<Preview>
  <Story name="TextField with label and statuses" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <Stack>
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'error'}
          styleType={'filled'}
        />
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'error'}
          styleType={'outlined'}
        />
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'error'}
          styleType={'elevated'}
        />
      </Stack>
      <Stack>
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'hint'}
          styleType={'filled'}
        />
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'hint'}
          styleType={'outlined'}
        />
        <TextField
          hintMsg={text('custom error message', 'Error in Text Field')}
          label={'Label'}
          status={'hint'}
          styleType={'elevated'}
        />
      </Stack>
    </Stack>
  </Story>
</Preview>

# Text Field with KNOBS

Regular Text Field with KNOBS

<Preview>
  <Story name="Text Field with KNOBS" parameters={{ decorators: [withKnobs] }}>
    <Stack>
      <TextField
        label={text('Label', 'Label')}
        size={select('Size', ['md','sm'], 'md')}
        lean={boolean('Lean version', false)}
        disabled={boolean('Disabled', false)}
        iconName={select('Icon',['none', ...Object.keys(iconSelector)],'search')}
        styleType={select('Style', ['filled', 'outlined', 'elevated'], 'filled')}
        hintMsg={text('custom error message', 'Error in Text Field')}
        status={select('status', ['hint', 'error', 'normal'], 'normal')}
      />
    </Stack>
  </Story>
</Preview>

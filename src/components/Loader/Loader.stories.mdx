import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import Loader from './Loader';
import {loaderTypes} from './Loader.utils';

<Meta title="Design System/Loaders" component={Loader} />

# Loader

A universal Loader component with different types.

# Usage

```js
import { Loader } from '@orfium/ictinus';

<Loader type="dots" />;
```

# Props

<Props of={Loader} />

# Available Loaders

Loader components.

<Preview>
  <Story name="Available Loaders">
    {loaderTypes.map(type => <div style={{margin: 10}}><strong>{type}: </strong><Loader type={type} /></div>)}
  </Story>
</Preview>

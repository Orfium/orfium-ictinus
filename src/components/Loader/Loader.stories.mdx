import { Meta, Story, Preview, Props } from '@storybook/addon-docs';

import Loader from './Loader';
import { FIGMA_URL } from '../../utils/common';

<Meta title="Design System/Loader" component={Loader} parameters={{
  design: {
    type: 'figma',
    url: `${FIGMA_URL}?node-id=2992%3A63789`,
  },
}} />

# Loader

A universal Loader component with different types.

# Usage

```js
import { Loader } from '@orfium/ictinus';

<Loader type="dots" />;
```

# Props

<Props of={Loader} />

# Loader Types

Available Loader components.

<Preview>
  <Story name="Loader Types">
    {['dots', 'indeterminate', 'spinner'].map(type => (
      <div style={{ margin: 10 }}>
        <strong>{type}: </strong>
        <Loader type={type} />
      </div>
    ))}
  </Story>
</Preview>

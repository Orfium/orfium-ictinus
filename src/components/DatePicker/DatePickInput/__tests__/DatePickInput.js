describe('Change Locale Test Suite', () => {
  let navigator;
  beforeEach(() => {
    navigator = {
      languages: ['el', 'en'],
    };
  });
  it('should display the correct locale', () => {});
});

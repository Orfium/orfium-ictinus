import { Meta, Preview, Props, Story } from '@storybook/addon-docs/blocks';
import Radio, { RadioWithoutForwardRef } from './Radio';
import RadioButtonsShowcase from '../storyUtils/RadioButtonsShowcase';
import { boolean, select, withKnobs } from '@storybook/addon-knobs';
import Stack from '../storyUtils/Stack';

<Meta title="Design System/Radio" component={Radio} />

# Radio

A custom made radio input based on the Orfium design system. Passing a `ref` will return the actual input element.
If the `checked` prop is not defined it means the `Radio` is not controlled.

# Usage

```jsx
import { Radio } from '@orfium/ictinus';

<Radio checked={value === 'a'} value={'a'} onChange={() => {}} />;
```

# Props

<Props of={RadioWithoutForwardRef} />

# Not selected Radio (`checked === undefined`)

<Preview>
  <Story name="Not selected Radio  (`checked === undefined`)">
    <Stack>
      <Radio />
      <Radio filled={false} />
    </Stack>
  </Story>
</Preview>

# Not selected Radio (`checked === false`)

<Preview>
  <Story name="Not selected Radio (`checked === false`)">
    <Stack>
      <Radio checked={false} />
      <Radio filled={false} checked={false} />
    </Stack>
  </Story>
</Preview>

# Selected Radio

<Preview>
  <Story name="Selected Radio">
    <Radio checked={true} />
  </Story>
</Preview>

# Disabled not selected Radio

<Preview>
  <Story name="Disabled not selected Radio">
    <Stack>
      <Radio checked={false} disabled={true} />
      <Radio filled={false} checked={false} disabled={true} />
    </Stack>
  </Story>
</Preview>

# Disabled selected Radio

<Preview>
  <Story name="Disabled selected Radio">
    <Radio checked={true} disabled={true} />
  </Story>
</Preview>

# Radio group

<Preview>
  <Story name="Radio showcase">
    <RadioButtonsShowcase />
  </Story>
</Preview>


# Radio Playground

<Preview>
  <Story name="Radio Playground"  parameters={{ decorators: [withKnobs] }}>
    <Radio checked={boolean("checked", true)} disabled={boolean("disabled", false)}  filled={boolean("filled", false)} />
  </Story>
</Preview>

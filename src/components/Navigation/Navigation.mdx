import { Meta, Canvas, ArgTypes } from '@storybook/blocks';
import Navigation from './Navigation';
import { FIGMA_URL } from '../../utils/common';
import * as NavigationStories from './Navigation.stories';

<Meta of={NavigationStories} />

<SectionHeader title={'Navigation'} isPending />

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)

## Overview

A universal Navigation component

## Props

<ArgTypes of={Navigation} />

## Usage

```js
import { Navigation } from '@orfium/ictinus';

<Navigation expanded={expanded} menuItems={menuItems} />;
```

<Tip>
  When rendering the Navigation component, the parent component should have `position: relative` in
  order for the Navigation to align correcly since it has `position: absolute`.
</Tip>

<SubsectionHeader title="Variants" />

### Navigation

<Canvas of={NavigationStories.NavigationStory} />

### Navigation with custom component

<Canvas of={NavigationStories.NavigationWithCustomComponent} />

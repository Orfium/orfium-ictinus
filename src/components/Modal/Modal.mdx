import { Meta, Canvas, ArgTypes } from '@storybook/blocks';
import Modal from './Modal';
import ModalContent from './ModalContent';
import * as ModalStories from './Modal.stories';

<Meta of={ModalStories} />

<SectionHeader title={'Modal'} isPending />

- [Overview](#overview)
- [Props](#props)

## Overview

A modal is used to reveal critical information, show information without losing context, or when the system requires a user response.

## Props

<ArgTypes of={Modal} />

## Usage

```js
import { Modal } from '@orfium/ictinus';

<Modal isOpen={true} onClose={closeCallback}>
  {'some content here'}
</Modal>;
```

<Canvas of={ModalStories.Modal} />

### Modal Content

### Props

<ArgTypes of={ModalContent} />

### Usage

```js
import { Modal, ModalContent } from '@orfium/ictinus';

<Modal
  isOpen={true}
  onClose={closeFunc}
  contentProps={{
    label: 'Label',
    heading: 'Heading',
    message:
      'Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas tristique eros at fringilla fringilla. Donec volutpat lobortis euismod',
    primaryCTALabel: 'Do Action',
    primaryCTA: actionCallback,
    secondaryCTALabel: 'Cancel',
    secondaryCTA: cancelCallback,
  }}
></Modal>;
```

<Canvas of={ModalStories.ModalContent} />

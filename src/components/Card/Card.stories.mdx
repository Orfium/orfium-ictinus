import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { select } from '@storybook/addon-knobs';
import CardShowcase from '../storyUtils/CardShowcase';
import Card from './Card';

<Meta title="Design System/Card" component={Card} />

# Card

A universal Card component that is wrapper for other components

# Usage

```js
import { Card } from '@orfium/ictinus';

<Card elevated={'01'}>
  <div>Content</div>
</Card>;
```

# Props

<Props of={Card} />

# Card with Elevation

Card with Elevation

<Preview>
  <Story name="Card with elevation">
    <Stack>
      <CardShowcase elevated={select('elevated', [undefined, '01', '02', '03', '04'], '01')} />
    </Stack>
  </Story>
</Preview>

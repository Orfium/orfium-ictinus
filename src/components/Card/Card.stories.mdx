import { Meta, Story, Preview, Props } from '@storybook/addon-docs';
import { select, boolean } from '@storybook/addon-knobs';
import CardShowcase from '../storyUtils/CardShowcase';
import Card from './Card';
import Stack from '../storyUtils/Stack';
import { FIGMA_URL } from '../../utils/common';

<Meta
    title="Design System/Card"
    component={Card}
    parameters={{
        design: [{
            type: 'figma',
            name: 'Card',
            url: `${FIGMA_URL}?node-id=9714%3A110184`,
        }],
    }} />

# Card

A universal Card component that is wrapper for other components

# Usage

```js
import { Card } from '@orfium/ictinus';

<Card elevated={'01'}>
  <div>Content</div>
</Card>;
```

# Props

<Props of={Card} />

# Card with Elevation

Card with Elevation

<Preview>
  <Story name="Card with elevation">
    <Stack>
      <CardShowcase
        elevated={select('elevated', [undefined, '01', '02', '03', '04'], '01')}
        isTransparent={boolean('isTransparent', false)}
        radius={select('radius', [undefined, 'xsm', 'sm', 'md', 'lg', 'xl'])}
      />
    </Stack>
  </Story>
</Preview>

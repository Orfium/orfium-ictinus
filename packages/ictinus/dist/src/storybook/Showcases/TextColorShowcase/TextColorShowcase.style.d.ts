import { SerializedStyles } from '@emotion/react';
import { Theme } from 'theme';
export declare const dividerStyle: () => SerializedStyles;
export declare const descriptionStyle: (colorCategory: string) => (theme: Theme) => SerializedStyles;

import { default as React } from 'react';
type CustomTooltipProps = {
    content: React.ReactNode;
    fill: string;
};
declare const CustomTooltip: React.FCC<CustomTooltipProps>;
export default CustomTooltip;

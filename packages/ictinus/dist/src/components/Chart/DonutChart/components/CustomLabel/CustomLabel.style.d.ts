import { SerializedStyles } from '@emotion/react';
import { Theme } from 'theme';
export declare const flexContainer: () => () => SerializedStyles;
export declare const labelUnitStyle: () => (theme: Theme) => SerializedStyles;

import { SerializedStyles } from '@emotion/react';
import { Theme } from '../../../../theme';
export declare const containerStyles: (gap: string) => () => SerializedStyles;
export declare const contentStyles: () => (theme: Theme) => SerializedStyles;

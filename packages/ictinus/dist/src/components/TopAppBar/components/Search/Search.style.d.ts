import { SerializedStyles } from '@emotion/react';
import { Theme } from '../../../../theme';
export declare const searchWrapper: (isDark: boolean) => (theme: Theme) => SerializedStyles;
export declare const customInputStyle: (searchPlaceholder: string, isDark: boolean) => (theme: Theme) => SerializedStyles;

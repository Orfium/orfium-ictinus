import { SerializedStyles } from '@emotion/react';
import { Theme } from '../../../../theme';
declare const _default: {
    buttonTextStyle: (theme: Theme) => SerializedStyles;
};
export default _default;

import { SerializedStyles } from '@emotion/react';
import { Theme } from '../../../../theme';
export declare const placeholderWrapper: SerializedStyles;
declare const _default: {
    wrapper: (theme: Theme) => SerializedStyles;
    placeholderWrapper: SerializedStyles;
};
export default _default;

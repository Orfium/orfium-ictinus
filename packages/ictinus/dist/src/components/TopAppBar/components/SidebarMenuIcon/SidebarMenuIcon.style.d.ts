import { SerializedStyles } from '@emotion/react';
import { Theme } from '../../../../theme';
declare const _default: {
    iconWrapper: (theme: Theme) => SerializedStyles;
};
export default _default;

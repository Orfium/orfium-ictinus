import { SerializedStyles } from '@emotion/react';
import { CardProps } from './Card';
import { Theme } from '../../theme';
export declare const cardStyle: ({ elevated, isTransparent, radius }: CardProps) => (theme: Theme) => SerializedStyles;

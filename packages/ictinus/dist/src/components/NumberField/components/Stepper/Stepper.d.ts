import { default as React } from 'react';
type Props = {
    size?: 'normal' | 'compact';
    isDisabled?: boolean;
    dataTestIdPrefix?: string;
};
declare const Stepper: React.FCC<Props>;
export default Stepper;

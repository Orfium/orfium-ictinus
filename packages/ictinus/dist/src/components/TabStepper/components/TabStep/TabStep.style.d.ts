import { CSSObject } from '@emotion/react';
import { Theme } from '../../../../theme';
export declare const stepStyles: (sx?: CSSObject) => import('@emotion/utils').SerializedStyles;
export declare const stepContainer: () => (theme: Theme) => import('@emotion/utils').SerializedStyles;
export declare const stepTitle: () => (theme: Theme) => import('@emotion/utils').SerializedStyles;
export declare const stepSubtitle: () => (theme: Theme) => import('@emotion/utils').SerializedStyles;

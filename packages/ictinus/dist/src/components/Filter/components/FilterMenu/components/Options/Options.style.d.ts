import { Theme } from 'theme';
export declare const optionsStyles: ({ isMulti }: {
    isMulti: any;
}) => () => import('@emotion/utils').SerializedStyles;
export declare const emptyStyle: () => (theme: Theme) => import('@emotion/utils').SerializedStyles;

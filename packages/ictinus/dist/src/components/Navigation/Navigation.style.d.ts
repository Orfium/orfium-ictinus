import { SerializedStyles } from '@emotion/react';
import { Theme } from '../../theme';
export declare const navigationContainerStyle: (isExpanded: boolean, isDesktop: boolean, isSmallDesktop: boolean) => (theme: Theme) => SerializedStyles;

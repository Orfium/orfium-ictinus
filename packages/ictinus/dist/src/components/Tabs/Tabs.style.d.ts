import { Theme } from '@emotion/react';
import { TabOrientation } from './types';
export declare const tagStyles: (isActive?: boolean) => (theme: Theme) => import('@emotion/utils').SerializedStyles;
export declare const showcaseContent: (orientation: TabOrientation) => (theme: Theme) => import('@emotion/utils').SerializedStyles;

import { CSSObject } from '@emotion/react';
import { TabOrientation } from '../../types';
export declare const tabsContainerStyles: (orientation: TabOrientation, sx?: CSSObject) => import('@emotion/utils').SerializedStyles;

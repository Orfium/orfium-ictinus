import { CSSObject } from '@emotion/react';
import { Theme } from '../../../../theme';
export declare const containerStyles: (sx?: CSSObject) => (theme: Theme) => import('@emotion/utils').SerializedStyles;

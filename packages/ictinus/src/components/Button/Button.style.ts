import { vars } from '@orfium/tokens';

export const buttonSpanStyle = () => {
  return {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    gap: vars.spacing['4'],
  };
};
